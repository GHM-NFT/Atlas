# GHM Atlas Command Centre v31+ RC3.1 — browser-upload snapshot

This folder is the browser-hosted counterpart of the RC3.1 local review build.

## GitHub browser upload

Upload the contents of this folder to the repository path you intend to publish. The package contains fewer than 100 files so it can be uploaded through GitHub’s browser interface.

After GitHub Pages deploys, open:

`https://<account>.github.io/<repository>/GHM_Atlas_Command_Centre_v31_Plus_RC3_1_Responsive_Corrections_GitHub_Browser_Upload/index.html#overview`

Use the exact uploaded folder name in the URL.

## Data and privacy

This package contains the same embedded 21 July 2026 snapshot as the local build. It does not connect to Google Sheets and does not write data back. Hosting it publicly makes the embedded snapshot publicly downloadable, so only publish it where that exposure is approved.

## Provisional percentages

The current snapshot has no authoritative progress percentage. RC3.1 displays deterministic visual placeholders labelled **provisional**. The Internal Audit progress field displays **50% — Provisional placeholder**.

## Review

A fresh Chrome visual review is required at 1920 × 1080, 1366 × 768, tablet portrait/landscape and 390 × 844. Replacement clean icons remain pending.
