# GHM Atlas Command Centre v31+ RC3.2 — browser-upload snapshot

This folder is the browser-hosted counterpart of the RC3.2 local review build.

## GitHub browser upload

Upload the contents of this folder to the repository path you intend to publish. The package contains fewer than 100 files, so it can be uploaded through GitHub’s browser interface.

After GitHub Pages deploys, open:

`https://<account>.github.io/<repository>/GHM_Atlas_Command_Centre_v31_Plus_RC3_2_Final_Corrections_GitHub_Browser_Upload/index.html#overview`

Use the exact uploaded folder name in the URL.

## Data and privacy

This package contains the same embedded 21 July 2026 snapshot as the local build. It does not connect to Google Sheets and does not write data back. Hosting it publicly makes the embedded snapshot publicly downloadable, so only publish it where that exposure is approved.

## RC3.2 corrections

- Bird’s-eye uses a small top-left title box, bottom controls, a visible map background and touch-safe nodes.
- Overview emblem dimensions remain stable after route changes.
- Waffle uses a contiguous multicolour wall and restored equal-stroke item cards.
- Territory pages use the angled-line texture and a unified red/orange status hierarchy.
- Progress values are compact provisional visual placeholders.
- Quick Chamber information uses two columns except on phone widths.
- The supplied orange and turquoise wax seals are included.

## Outstanding

The clean replacement primary navigation and territory icons are still to be supplied and integrated.
