# GitHub Upload — v30.0

Upload the complete folder:

`GHM_Atlas_Command_Centre_Map_v30.0`

Expected live route:

`https://ghm-nft.github.io/Atlas/GHM_Atlas_Command_Centre_Map_v30.0/index.html#overview`

Do not flatten or rename the internal `assets`, `css` or `js` folders.

The external font request is optional at runtime; fallback fonts are defined in CSS.

No generated imagery is included.
